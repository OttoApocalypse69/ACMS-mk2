<UserControl x:Class="PixelArtEditor.UI.Views.TimelineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <Grid Background="#252526">
        <Grid.RowDefinitions>
            <RowDefinition Height="24"/> <!-- Toolbar -->
            <RowDefinition Height="*"/>  <!-- Frames -->
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Background="#333333">
            <Button Content="Play" Margin="2" Padding="5,0"/>
            <Button Content="Stop" Margin="2" Padding="5,0"/>
            <TextBlock Text="Timeline Placeholder" VerticalAlignment="Center" Foreground="#AAAAAA" Margin="10,0"/>
        </StackPanel>
        
        <Border Grid.Row="1" BorderBrush="#3E3E42" BorderThickness="0,1,0,0">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <StackPanel Orientation="Horizontal">
                    <!-- Frame Placeholders -->
                    <Border Width="40" Height="60" Margin="2" Background="#1E1E1E" BorderBrush="#007ACC" BorderThickness="1">
                        <TextBlock Text="1" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <Border Width="40" Height="60" Margin="2" Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="1">
                        <TextBlock Text="2" Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <Border Width="40" Height="60" Margin="2" Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="1">
                        <TextBlock Text="3" Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
